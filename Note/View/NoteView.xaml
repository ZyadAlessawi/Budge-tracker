<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Note.View.NoteView"
             xmlns:viewmodel="clr-namespace:Note.ViewModel" 
             xmlns:local="clr-namespace:Note.ViewModel"
             x:DataType="local:NoteViewModel"
             xmlns:data="clr-namespace:Note.Model">
    
   
    <!--<ContentPage.ToolbarItems>
        --><!--<ToolbarItem Order="Secondary" Text="Change Theme " Clicked="Change_Themese_Clicked" x:Name="Change_theme" />--><!--
        <ToolbarItem IconImageSource="add.png" Order="Primary" Text="History" Clicked="InsertPage_ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>
    <ScrollView>
        <StackLayout Padding="15">

            <Label  
                FontSize="Medium"
                Text="Note "
                HorizontalOptions="Center" />



            <StackLayout>
                <CollectionView 
       
                    x:Name="ListViewNote"
                    ItemsSource="{Binding Source={x:Reference ListViewNote}}" 
                    SelectedItem="{Binding SelectedNote}">
                    --><!--SelectedItems="{Binding ListViewNote_ItemSelected}"--><!--


                    <CollectionView.ItemTemplate>

                        <DataTemplate  x:DataType="data:Notes" >

                            <Frame>
                                <VerticalStackLayout>
                                    <Label Text="{Binding Title}"/>
                                    <Label Text="{Binding Description}"/>
                                </VerticalStackLayout>
                            </Frame>

                        </DataTemplate>

                    </CollectionView.ItemTemplate>

                </CollectionView>

            </StackLayout>



        </StackLayout>
    </ScrollView>-->


    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="Add_Clicked" IconImageSource="{FontImage Glyph='+', Color=White, Size=22}" Order="Primary" />
    </ContentPage.ToolbarItems>

    <CollectionView x:Name="notesCollection"
                    ItemsSource="{Binding Notes}"
                    Margin="20"
                    SelectionMode="None">

        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
        </CollectionView.ItemsLayout>

        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="data:Notes">
                <Border Stroke="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10" />
                    </Border.StrokeShape>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItem Text="Remove"
                                       BackgroundColor="#dd0000"
                                       Invoked="RemoveNote_Invoked"
                                       IconImageSource="{FontImage Glyph='🗑️', Color=White, Size=100}" />
                        </SwipeView.RightItems>

                        <Frame>
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Tapped="NoteCard_Tapped" />
                            </Frame.GestureRecognizers>
                            <Grid ColumnDefinitions="*">
                                <StackLayout>
                                    <Label Text="{Binding Text}" FontSize="22" LineBreakMode="TailTruncation" MaxLines="1" />
                                    <Label Text="{Binding Date}" FontSize="14" TextColor="Silver" />
                                </StackLayout>
                            </Grid>
                        </Frame>
                    </SwipeView>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>